<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Tournament Invitation - {{tournamentName}}</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #f6f6f6; padding: 20px; color: #333; }
        .container { max-width: 600px; margin: auto; background: #ffffff; padding: 30px; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.05); }
        .header { text-align: center; border-bottom: 2px solid #007bff; padding-bottom: 20px; margin-bottom: 30px; }
        .logo { font-size: 24px; font-weight: bold; color: #007bff; }
        .tournament-details { background: #f8f9fa; padding: 20px; border-radius: 5px; margin: 20px 0; }
        .detail-row { display: flex; justify-content: space-between; margin: 10px 0; }
        .detail-label { font-weight: bold; color: #555; }
        .detail-value { color: #333; }
        .cta-button { display: inline-block; background: #007bff; color: white; padding: 12px 25px; text-decoration: none; border-radius: 5px; margin: 20px 0; }
        .footer { text-align: center; margin-top: 30px; color: #666; font-size: 14px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">🏆 RMS Tournament System</div>
        </div>
        
        <h2 style="color: #333;">You're Invited to {{tournamentName}}!</h2>
        
        <p>Dear {{recipientName}},</p>
        
        <p>We're excited to invite you to participate in our upcoming robotics tournament!</p>
        
        <div class="tournament-details">
            <h3 style="margin-top: 0; color: #007bff;">Tournament Details</h3>
            <div class="detail-row">
                <span class="detail-label">Tournament:</span>
                <span class="detail-value">{{tournamentName}}</span>
            </div>
            {{#if description}}
            <div class="detail-row">
                <span class="detail-label">Description:</span>
                <span class="detail-value">{{description}}</span>
            </div>
            {{/if}}
            <div class="detail-row">
                <span class="detail-label">Start Date:</span>
                <span class="detail-value">{{startDate}}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">End Date:</span>
                <span class="detail-value">{{endDate}}</span>
            </div>
            {{#if location}}
            <div class="detail-row">
                <span class="detail-label">Location:</span>
                <span class="detail-value">{{location}}</span>
            </div>
            {{/if}}
            {{#if maxTeams}}
            <div class="detail-row">
                <span class="detail-label">Max Teams:</span>
                <span class="detail-value">{{maxTeams}}</span>
            </div>
            {{/if}}
        </div>
        
        <p>This is a great opportunity to showcase your team's skills and compete with other talented robotics teams!</p>
        
        {{#if registrationUrl}}
        <div style="text-align: center;">
            <a href="{{registrationUrl}}" class="cta-button">Register Your Team</a>
        </div>
        {{/if}}
        
        <p>If you have any questions about the tournament, please don't hesitate to contact us.</p>
        
        <div class="footer">
            <p>Best regards,<br />
            <strong>{{adminName}}</strong><br />
            Tournament Administrator<br />
            The STEAM Team</p>
            
            {{#if contactEmail}}
            <p>Contact: <a href="mailto:{{contactEmail}}">{{contactEmail}}</a></p>
            {{/if}}
        </div>
    </div>
</body>
</html>
