<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Tournament Results - {{tournamentName}}</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #f6f6f6; padding: 20px; color: #333; }
        .container { max-width: 700px; margin: auto; background: #ffffff; padding: 30px; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.05); }
        .header { text-align: center; border-bottom: 2px solid #ffc107; padding-bottom: 20px; margin-bottom: 30px; }
        .logo { font-size: 24px; font-weight: bold; color: #ffc107; }
        .podium { display: flex; justify-content: center; align-items: end; margin: 30px 0; }
        .podium-place { text-align: center; margin: 0 10px; padding: 20px; border-radius: 10px; }
        .podium-place.first { background: linear-gradient(135deg, #ffd700, #ffed4e); order: 2; height: 120px; }
        .podium-place.second { background: linear-gradient(135deg, #c0c0c0, #e5e5e5); order: 1; height: 100px; }
        .podium-place.third { background: linear-gradient(135deg, #cd7f32, #daa520); order: 3; height: 80px; }
        .podium-number { font-size: 36px; font-weight: bold; margin-bottom: 10px; }
        .podium-team { font-size: 18px; font-weight: bold; margin-bottom: 5px; }
        .podium-score { font-size: 14px; color: #666; }
        .rankings-table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        .rankings-table th, .rankings-table td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        .rankings-table th { background: #f8f9fa; font-weight: bold; }
        .rankings-table .rank { font-weight: bold; width: 60px; text-align: center; }
        .rankings-table .team-highlight { background: #fff3cd; }
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin: 20px 0; }
        .stat-card { background: #f8f9fa; padding: 20px; border-radius: 8px; text-align: center; }
        .stat-number { font-size: 32px; font-weight: bold; color: #007bff; margin-bottom: 5px; }
        .stat-label { font-size: 14px; color: #666; }
        .award { background: #e8f5e8; border-left: 4px solid #28a745; padding: 15px; margin: 10px 0; border-radius: 0 5px 5px 0; }
        .award-title { font-weight: bold; color: #28a745; margin-bottom: 5px; }
        .congratulations { background: linear-gradient(135deg, #007bff, #28a745); color: white; padding: 25px; border-radius: 10px; text-align: center; margin: 30px 0; }
        .footer { text-align: center; margin-top: 30px; color: #666; font-size: 14px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">🏆 Tournament Results</div>
        </div>
        
        <h2 style="color: #333; text-align: center;">{{tournamentName}} - Final Results</h2>
        
        {{#if isWinner}}
        <div class="congratulations">
            <h2 style="margin: 0; font-size: 28px;">🎉 CONGRATULATIONS! 🎉</h2>
            <p style="margin: 10px 0 0 0; font-size: 18px;">{{teamName}} finished in {{finalRank}} place!</p>
        </div>
        {{/if}}
        
        <p>Dear {{teamName}} Team,</p>
        
        <p>The {{tournamentName}} has concluded! Thank you for your outstanding participation and sportsmanship throughout the tournament.</p>
        
        {{#if topThree}}
        <div class="podium">
            {{#each topThree}}
            <div class="podium-place {{this.position}}">
                <div class="podium-number">{{this.rank}}</div>
                <div class="podium-team">{{this.teamNumber}}</div>
                <div class="podium-team">{{this.teamName}}</div>
                <div class="podium-score">{{this.totalPoints}} pts</div>
            </div>
            {{/each}}
        </div>
        {{/if}}
        
        <h3>Your Team's Performance</h3>
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number">{{teamStats.finalRank}}</div>
                <div class="stat-label">Final Rank</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{teamStats.totalMatches}}</div>
                <div class="stat-label">Matches Played</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{teamStats.wins}}</div>
                <div class="stat-label">Wins</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{teamStats.averageScore}}</div>
                <div class="stat-label">Avg Score</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{teamStats.highestScore}}</div>
                <div class="stat-label">Highest Score</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{teamStats.rankingPoints}}</div>
                <div class="stat-label">Ranking Points</div>
            </div>
        </div>
        
        {{#if awards}}
        <h3>Awards & Recognition</h3>
        {{#each awards}}
        <div class="award">
            <div class="award-title">🏅 {{this.name}}</div>
            <div>{{this.description}}</div>
        </div>
        {{/each}}
        {{/if}}
        
        {{#if showFullRankings}}
        <h3>Final Rankings</h3>
        <table class="rankings-table">
            <thead>
                <tr>
                    <th class="rank">Rank</th>
                    <th>Team</th>
                    <th>Team Name</th>
                    <th>W-L-T</th>
                    <th>RP</th>
                    <th>Avg Score</th>
                </tr>
            </thead>
            <tbody>
                {{#each finalRankings}}
                <tr class="{{#if this.isCurrentTeam}}team-highlight{{/if}}">
                    <td class="rank">{{this.rank}}</td>
                    <td>{{this.teamNumber}}</td>
                    <td>{{this.teamName}}</td>
                    <td>{{this.wins}}-{{this.losses}}-{{this.ties}}</td>
                    <td>{{this.rankingPoints}}</td>
                    <td>{{this.averageScore}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}
        
        {{#if tournamentStats}}
        <h3>Tournament Statistics</h3>
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number">{{tournamentStats.totalMatches}}</div>
                <div class="stat-label">Total Matches</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{tournamentStats.totalTeams}}</div>
                <div class="stat-label">Teams Participated</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{tournamentStats.highestScore}}</div>
                <div class="stat-label">Tournament High Score</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{tournamentStats.averageScore}}</div>
                <div class="stat-label">Tournament Average</div>
            </div>
        </div>
        {{/if}}
        
        <p style="margin-top: 40px;">We hope you had a fantastic experience competing in {{tournamentName}}. Your dedication, creativity, and teamwork made this tournament a huge success!</p>
        
        {{#if nextTournament}}
        <div style="background: #e3f2fd; padding: 20px; border-radius: 8px; margin: 20px 0;">
            <h4 style="margin-top: 0; color: #1976d2;">🚀 Next Tournament</h4>
            <p><strong>{{nextTournament.name}}</strong><br />
            {{nextTournament.date}} | {{nextTournament.location}}</p>
            <p>Registration opens: {{nextTournament.registrationOpen}}</p>
        </div>
        {{/if}}
        
        <div class="footer">
            <p>Thank you for being part of {{tournamentName}}!<br />
            <strong>{{organizerName}}</strong><br />
            Tournament Organizer</p>
            
            {{#if certificateUrl}}
            <p><a href="{{certificateUrl}}" style="color: #007bff;">📜 Download Participation Certificate</a></p>
            {{/if}}
            
            <p style="font-size: 12px; color: #999; margin-top: 20px;">
                Share your tournament experience on social media with #{{hashtag}}
            </p>
        </div>
    </div>
</body>
</html>
